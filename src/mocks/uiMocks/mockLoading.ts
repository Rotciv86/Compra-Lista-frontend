import { UiState } from "../../types/types";

export const mockInitialStateLoading: UiState = {
  feedback: {
    isOpen: true,
    messageFeedback: "",
    isError: true,
  },
  isLoading: false,
};
